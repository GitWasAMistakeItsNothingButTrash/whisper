
# Filepaths
inputfile = 
transcriptionfile = 
outputfile = 


input = open(inputfile,"r")
transcription = open(transciptionfile,"w")

timestamps = []

linecount = 1

for line in input:
	if linecount%3==0:
		timestamps.append(line)
	elif (linecount-1)%3==0:
		if linecount != 0: 
!!!			write line to transcription
	linecount += 1
	
input.close()
transcription.close()	

!!! Translate transcriptionfile --> translationfile

translation = open(translationfile,"r")
output = open(outputfile,"w")

!!! Write format header and empty line to output

for line in range(len(timestamps)):
!!!	write timestamps[line] to output
!!!	write translation[line] to output	

translation.close()
output.close()
